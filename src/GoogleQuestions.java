
public class GoogleQuestions {
	
	//1. return a traversable structure, class folders, has instance of its id and parent id. You are given a list of folders, must create a traversable data structure. root may be out of order.
	//2. roomba vacuum every possible open space
	//3. given a file, 10s and thousands of lines, goal: truncate the file "there are 1000 similar lines"
	//4. write a library that publishes events., subscribe, unsubscribe, 
	//5. remove every other element from a circular linked list

}
